#pragma once

/// BIOME ///
/// \brief not fully implemented - for now it's just the type; eventually this will be a struct containing a list of tile indices for one contiguous biome region
///
enum class Biome {
    GRASSLAND,
    FOREST,
    BEACH,
    LAKE,
    RIVER,
    OCEAN,
    VOLCANO,
    CALDERA,
    NULL_TYPE
};


